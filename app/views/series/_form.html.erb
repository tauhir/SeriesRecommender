<%= form_with(model: series, local: true) do |form| %>
  <% if series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(series.errors.count, "error") %> prohibited this series from being saved:</h2>

      <ul>
        <% series.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :api_id, %>
    <%= form.text_field :api_id, %>
  </div>

  <div class="field">
    <%= form.label :name,language,origin_country %>
    <%= form.text_field :name,language,origin_country %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
